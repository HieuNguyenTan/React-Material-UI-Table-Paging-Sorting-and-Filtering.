import { configureStore } from '@reduxjs/toolkit';
import { combineReducers } from 'redux'
import hhhhh from './bbb';

const reducer = combineReducers({
	hhhhh,
});

const store = configureStore({
    reducer,
  })
  
export default store;
